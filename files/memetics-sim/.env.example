# Server only. Never use a VITE_ prefix for credentials.
# Qwen hosted example; select the regional endpoint and model for your account.
SEMANTIC_BASE_URL=https://dashscope-intl.aliyuncs.com/compatible-mode/v1
SEMANTIC_MODEL=qwen-plus
SEMANTIC_API_KEY=
# For an already running local OpenAI-compatible model server, use its /v1 base
# URL and exact served model name; the API key may be empty if not required.
